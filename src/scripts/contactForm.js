// A ContactForm component that, when filled out and a submit button is pressed, adds a new contact to storage. It should import the ContactCollection component.

/* to do:
build HTML form
attach event listeners to the form button
append form to the DOM
when event happens - a) get user input, b) create an object which captures form info, c) post contact to JSON
*/